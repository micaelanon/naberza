export { prisma } from "./prisma-client";

// Note: Prisma-generated enums (Priority, TaskStatus, etc.) are only available
// after running `prisma generate`. Domain TypeScript enums live in src/types/domain.types.ts
// and are kept in sync with the Prisma schema manually.
// Once Prisma is generated (Phase 1 with a real DB), you can re-export from @prisma/client here.
