import { ReactNode } from 'react';

export const metadata = {
  title: 'Automations | Naberza OS',
  description: 'Configure rules, triggers, and automation workflows with approval flow',
};

export default function AutomationsPage(): ReactNode {
  return (
    <div className="page-container">
      <h1>Automations</h1>
      <p>Configure rules, triggers, and automation workflows with approval flow</p>
    </div>
  );
}
